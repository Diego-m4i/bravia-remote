{
  "name": "pairbeam",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "agent": "node desktop-agent.js",
    "build:agent": "pkg desktop-agent.js --targets node18-win-x64 --output dist/PairBeam-Agent.exe",
    "build:server": "pkg server.js --targets node18-win-x64 --output dist/PairBeam-Server.exe",
    "build": "npm run build:server && npm run build:agent"
  },
  "dependencies": {
    "node-ssdp": "^4.0.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}
